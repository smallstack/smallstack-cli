language: node_js
dist: trusty
node_js: "6"
install:
    - npm install -g
script:
    - smallstack help
notifications:
  slack: $SLACK_TOKEN
  on_success: change
deploy:
  provider: npm
  email: "max@smallstack.io"
  api_key: $NPM_API_KEY
  on:
    tags: true
