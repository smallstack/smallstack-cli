{
    <% for (var i = 0; i < packages.length; i++) { %>
    "<%= packages[i] %>": {
		"git": "https://bitbucket.org/smallstack/smallstack-packages.git",
		"version": "<%= version %>",
        "path": "<%= packages[i] %>"
    }<% if(i !== (packages.length - 1)) { %>,<% } %>
    <% } %>
}